project_id = "bkyuxvschuwngtcdhsyg"

[functions.submit-mindmake-brief]
verify_jwt = false

[functions.enrich-company]
verify_jwt = true

# Public and unauthenticated: the daily read is published on /ai-gtm, and this
# matches the already-deployed setting.
[functions.get-ai-news]
verify_jwt = false

# Called by the browser, with the origin allowlist enforced in code.
[functions.mindmake-personal-read]
verify_jwt = false

# Called by pg_cron with a shared secret header, the pattern the project's
# other scheduled functions already use.
[functions.send-follow-ups]
verify_jwt = false

[functions.aa-price-snapshot]
verify_jwt = false
