# Mindmake rebuild state

## Current snapshot

Verified: 26 August 2026

This snapshot is the current delivery truth. The preserved rebuild chronicle below records how the work changed over time. It intentionally includes earlier names, offers, routes, visual directions and test results that no longer describe the product.

- Repository: `C:\Users\krish\dev\mindmaker`
- Branch: `codex/mindmake-homepage-mock`
- Starting revision for this local pass: `2dfc7121969280c705925f3db2fc4e40a83197de`
- Current business source: `MINDMAKE_CANON.md`
- Approved visual and interaction floor for the opening sequence, judgement thread and first CTRL proof: `prototypes/mindmake-judgement-thread-motion-study-v5.html`, SHA-256 `DE09D75C46EB660AD6148C1D7F5DD61E4F82031B48FCFE931CC3AE05C8126C81`.
- Frozen Brain and GTM gateway: `prototypes/mindmake-brain-gtm-gateway-candidate-7-v2.html`, `src/components/mindmake/BrainGtmGateway.tsx` and `src/styles/mindmake-gateway.css`. Its wording, composition and door-separation motion must not be reinterpreted during later work.
- V8 remains the production breadth reference for routes, testimonial range and proof outside the V5 study. Where both artifacts cover the same surface, V5 wins.
- Public model: two doors, Build Your AI Brain and Build Your AI GTM, which both lead into one privately priced 30-day proof.
- Main action: `Start here`. There is no public diary or public price.
- Lead path: company website, honest company read, one problem choice, one returned-time choice, useful preview and private download. The version-two work-email hand-off exists in source and remains fail-closed until its migration, Edge Function, security boundary and both email paths are proved in preview.
- Newsletter permission is separate and unticked. There is no automated nurture series.
- CTRL remains Mindmake's product and proof layer, not a third public offer.
- Production promotion is not authorised.

### Local implementation state

- The approved V5 opening, working-understanding act, judgement thread and first CTRL proof are integrated into the production shell. The separately frozen Brain and GTM gateway is integrated without reinterpretation. V8 still supplies the route, proof and testimonial breadth outside those approved surfaces.
- The homepage, two outcome routes, eight customer stories, six testimonials, three clearly labelled attendee brands, Media section, checked article archive, FAQ, worked example, contact, legal pages and unlisted alumni page are implemented.
- The homepage now uses one measured content edge from the wordmark through the opening act, gateway, Results, About and Media. The Brain and GTM gateway uses real CTRL media with no diagram backdrop. Homepage proof is one section with three result previews, labelled attendance proof and one separate six-item career testimonial deck.
- Mobile is recomposed for the phone rather than scaled down from desktop. It keeps one content edge, uses the visible canvas below the fixed header, respects safe areas and keeps every control at least 44 by 44 pixels. Phone Results and Media become horizontal snap decks. Short landscape Results becomes a side-by-side editorial index and snap deck; About becomes a three-part composition.
- At 900 pixels wide or below, the measured consent notice is part of the top chrome. The layout reserves its exact rendered height for the header, open menu, route opening canvas and frozen gateway. Acceptance removes the reserve and returns those surfaces to the full available canvas.
- The mobile menu fills the viewport below the header, locks page scroll, closes with Escape and returns focus. Horizontal rails reveal the next card, support swiping, keep controls fixed and report their explicit end state. Scroll-linked motion reverses with scroll and reduced-motion mode keeps the same meaning.
- Copy does not truncate, clip or form aggressive narrow stacks. Video remains user-controlled and uses `preload="none"`. The starting brief responds to `visualViewport`, the on-screen keyboard and safe-area insets; Back, close, browser history and Escape preserve a predictable journey.
- The starting brief resets private state on close, aborts browser requests, protects against late client results, bounds company research at 10 seconds and gives an honest retry or manual route.
- The default journey asks for no email, sends no visitor identity, stores no lead and makes no hand-off. It still sends the company domain to `enrich-company` so it can return the promised company read. The disabled version-two hand-off sends only the email, domain and allowlisted choice IDs to `submit-mindmake-brief`; the server owns research, recommendation assembly, verification and both final emails. It never uses the retired contact pipeline.
- The private migration, version-two Edge Function, visitor email and operator digest now exist in source. They have not been applied or deployed. No current local result proves a real inbox delivery, private-schema policy or production secret configuration.
- General contact opens a prefilled email draft and never claims the message was sent.
- Former public intake and testimonial URLs redirect to current Mindmake journeys. Their old source remains recoverable pending an approved cleanup.
- Every indexed article has real crawler HTML, metadata, canonical data, Open Graph data and Article structured data.
- Unknown routes now reach the host's not-found path instead of a blanket single-page rewrite. Preview must still prove the HTTP status.
- No live email, Supabase mutation, deployment, merge, domain change, deletion or production promotion has been performed.

### Local verification state

The build, route and browser counts below are the last complete integrated front-end checkpoint. They do not verify the later source-only V2 private hand-off or any preview environment.

- `npm run build`: passed, 2,509 modules, 21 indexed and prerendered routes.
- `npm test`: 93 passed, 0 failed across 16 files.
- `npm run lint`: 41 errors and 19 warnings, against the recorded baseline of 41 errors and 20 warnings. Active hardening changes lint cleanly.
- Responsive matrix: 147 of 147 route and width combinations passed.
- Full sitemap browser audit: 42 of 42 desktop and mobile combinations passed.
- Focused mobile sitemap audit: 63 of 63 combinations across all 21 indexed routes at 320 by 568, 390 by 844 and 844 by 390 completed without horizontal overflow, broken images or browser errors after genuine-issue triage.
- Guided brief, keyboard paths, mobile-menu focus containment, reduced motion, focus return, quote stability, CTRL video and error fallbacks passed with all network calls intercepted. The earlier enabled-mode V1 hand-off check is historical evidence only and does not validate the V2 contract.
- The latest focused homepage browser pass is clean at 1440 by 900, 1280 by 720, 1024 by 768, 844 by 390 and 390 by 844. It found no horizontal overflow, browser error, clipped result card or moving testimonial control. The navigation, hero, gateway, Results, About and Media share the exact same left edge at every size. At 844 by 390, the 320-pixel canvas below the header contains the 319-pixel Results section, 261-pixel About section and 228-pixel Media section without hiding their copy or actions. At 390 by 844, Results is 772 pixels, About is 742 pixels and Media is 757 pixels inside the 774-pixel canvas. The Results archive link and testimonial controls remain at least 44 pixels high. Reduced-motion short landscape keeps the two gateway choices side by side and both gateway videos paused.
- V5 SHA-256 was rechecked after integration and remains `DE09D75C46EB660AD6148C1D7F5DD61E4F82031B48FCFE931CC3AE05C8126C81`.
- Full evidence: `MINDMAKE_REBUILD_QA_2026-08-23.md`.

### Remaining gates

1. Apply the private-brief migration to preview; run database lint and security advisers; prove browser roles cannot read the private schema or call its RPC; deploy the version-two Edge Function; and exercise the complete request, verification and independent-delivery matrix with synthetic inboxes.
2. Agree the retention schedule and implement any promised cleanup before changing the hand-off flag. Confirm the current privacy, separate publication-interest and terms wording.
3. Physical iOS Safari and Android Chrome checks.
4. Publication URL and subscription decision.
5. Rebrand, archive or delete decision for the retained old intake and testimonial source.
6. Exact approval for legacy code and asset cleanup.
7. Preview deployment with real HTTP 404, sitemap and lead-flow verification.
8. Separate merge, domain and production-promotion approvals.
9. Preview enrichment smoke test with the intended environment. Local enrichment currently fails closed because `VITE_SUPABASE_URL` is not set. Configure exact allowed origins, sender, operator email and symbolic secret references without recording their values in documentation.
10. Remove the retired private money disclosure from deferred Supabase knowledge before that path can answer publicly.
11. Decide whether frozen V5 copy may keep the unexplained `ROI` short form or needs a separately approved plain-language change.

## Preserved rebuild chronicle

Last verified: 2026-08-26

> Everything below this notice is a dated build chronicle. Labels such as `Current truth`, `Locked product decisions`, `approved` and `next action` describe the checkpoint in which they were written. They are superseded by `Current snapshot` at the top of this file and must not be used as present offer, CTA, route, conversion or release guidance. Historical references to the Sprint, fit calls, Calendly, Mindy, the Diagnosis Room, `Improve what you sell`, the Teardown and the Handover are preserved only to explain how the rebuild changed.

## Preflight

- `STATE_ROUTE`: `project-documentation/REBUILD_STATE.md`
- `SOURCE_LAYERS`: The repositioning brief supplied on 12 August 2026 and Krish's approved interview answers supersede the earlier two-offer rebuild plan. The brands and testimonials document remains the proof source. `DECISIONS_LOG.md` and `HISTORY.md` are history. The live products and current repository revisions are external evidence.
- `PRODUCT_TRUTH`: Mindmaker helps leaders make consequential commercial decisions shaped by AI. The work protects and builds their expertise, judgement, and taste, then uses AI to make those strengths more useful. There is one public paid engagement, a focused Sprint. CTRL by Mindmaker is the living deliverable that helps the work endure, not a separate headline product.
- `NON_GOALS`: Do not position Mindmaker as an automation consultancy. Do not publish the Sprint price. Do not change Supabase, the control centre, or the CTRL repository. Do not promote to production. Do not discard Mindmaker's strongest visual identity, motion, owl, imagery, or interaction patterns without evidence.
- `SURFACE_DEPENDENCIES`: current-truth and product audit -> positioning interrogation -> offer, page journey, and copy lock -> Relume exploration -> code-faithful mock -> visual approval -> implementation -> preview verification -> merge.
- `VERTICAL_SLICE`: A first-time visitor understands that Krish helps resolve an important commercial decision shaped by AI, sees evidence that the work goes beyond a consulting deck, understands the 21-day Sprint and can book a fit call.
- `FIRST_SURFACE`: homepage.

## Current truth

- Repository: `https://github.com/krishanraja/mindmake`
- Local checkout: `C:\Users\krish\dev\mindmaker`
- Branch: `codex/mindmaker-rebuild`
- Starting revision: `3bee2b3ec0a5973f62c0f325514d98eb8ee3d877`
- Mindmaker production deployment: `dpl_AMGKvsMFgew6QXiCQ2fqqVLtyfXd`, built from the starting revision.
- CTRL production deployment: `dpl_EQDxunnZqt6v7rjiSoWQsx9fqoq3`, revision `c5021a0c0e97b71e7a973c8e3e4118115f8041f3`.
- Source brief: `C:\Users\krish\Downloads\# MINDMAKER REPOSITIONING BRIEF.txt`.
- Production promotion: not authorised.
- Existing `_corpus` directory is preserved and untracked.
- Dependency install required `npm ci --ignore-scripts` because `canvas` has no prebuilt binary for the available Windows ARM64 Node runtime.

## Baseline evidence

- `npm run build`: passed. Vite warned about an ambiguous easing class, an old Browserslist data set, and a large bundle.
- `npm test`: failed before changes. 29 tests passed and 6 failed across Windows path handling and local-storage test setup.
- `npm run lint`: failed before changes with 42 errors and 20 warnings.
- Locked dependency tree reports 18 audit findings: 1 low, 2 moderate, and 15 high. Dependency upgrades are outside this rebuild unless a finding blocks the requested work.

## Locked product decisions

- Lead with the decision. Do not lead with automation, implementation, training, CTRL, or a narrow sector.
- The commercial focus includes product strategy, corporate strategy, go-to-market strategy, and the organisation needed to support them as AI changes the market.
- Krish's strongest experience is in data, media, publishing, content, and intellectual property, but the main headline must remain useful to other strong-fit businesses such as coaching and consulting firms.
- Offer one paid, time-bound Sprint. Its final public name is not locked.
- CTRL is why the work lasts and proof that the engagement is not just a deck. It is not sold as an assistant, chief of staff, productivity tool, briefing service, or separate public plan.
- The hero has one clear booking action. The intelligent input and all Diagnosis Room UI are paused for this release so the product demonstration does not obscure Krish's judgement-led service.
- Use redacted CTRL screens as visual evidence where they can be shown safely.
- Preserve and improve the current design system. Keep the strongest motion, interaction, owl, editorial voice, dark and light contrast, and real-world imagery. Retire only what is generic, distracting, or tied to obsolete positioning.
- Use Tenex as a reference for pace, editorial scale, physical imagery, sticky storytelling, and contrast. Do not copy its statues, yellow palette, or fear-led copy.
- The visitor should feel able to navigate what comes next, protect themselves from change, build their own AI brain, and keep growing as an AI-native leader.
- The 90-day commercial result is 2 to 3 paying customers.
- The two urgent buyer triggers are: the business is at risk of being beaten by faster startups, or the business could grow faster but pricing, positioning, or another commercial constraint is holding it back.
- A buyer may begin with an ambiguous problem and no clear diagnosis. Mindmaker's nested interview systems must extract the leader's hidden knowledge, build a graph of what the uncertainty rests on, and expose the gap between stated intent and actual behaviour.
- For startup pressure and blocked growth, the first four diagnostic lenses are Product, Pricing, Positioning, and People. The Sprint identifies how each contributes to the problem.
- The public homepage must not become a long intake. It may offer a very small intelligent preview, then a call.
- The full predictive nested interview is a future replacement for `/intake`, shown only after serious interest. It is scoped but not approved for implementation in the current homepage build.
- If the homepage demonstration is resumed later, the last approved product shape was a two-input `Decision Underneath` experience. It is not part of the current release.
- The result names the decision underneath, why it may matter now, what may be causing it, one useful 21-day test, and the most important fact the system cannot know from outside.
- Krish approved the editorial decision-plate visual direction on 12 August 2026. It assembles visibly from evidence, is not a chatbot response, and can retain the same structure as a responsive private link and PDF.
- The first company-to-pressure flow in `prototypes/mindmaker-decision-artifact-v1.html?flow=1` proved the interaction but was rejected as the homepage design direction. Its Space Grotesk and Inter pairing and staged full-screen flow felt like a familiar AI-built site.
- The component-led homepage direction is now rendered at `prototypes/mindmaker-homepage-component-led-v2.html`. It uses a normal page scroll, asymmetric real-world imagery, CTRL evidence, an embedded Decision Underneath moment, and one proposed design language. Visual approval is pending.
- Krish's review of V2 found three material regressions: its intelligent tool was only a weak fixed stand-in, too many useful production features were no longer visible, and there were too few meaningful build moments. Its typography also mixed too many unrelated voices.
- The comparison-led V3 direction is rendered at `prototypes/mindmaker-homepage-v3.html`. It restores and reshapes production strengths: flip discovery, CTRL inspection, company co-branding, multiple company-specific sample reads, source marks, voice and haptic affordances, a persistent-memory demonstration, visible handback, expandable case files, optional media drawers, theme behaviour and the owl.
- V3 was rejected on 12 August 2026. It is frozen as negative evidence, not a candidate baseline. The primary failures were a generic component language, an empty first state, awkward oversized type, broken-feeling controls, weak build choreography and the loss of stronger production-era visual and interactive assets.
- The rebuild is now preservation-first. The current coded site is the visual and interaction asset library. No component is removed unless the replacement is shown side by side to be clearer, more distinctive, more delightful, at least as useful on mobile and no worse for conversion.
- The comparison gate lives in `project-documentation/HOMEPAGE_COMPARISON_MATRIX.md`. The motion contract lives in `project-documentation/HOMEPAGE_MOTION_STORYBOARD.md`. The switchable type test lives in `prototypes/mindmaker-typography-test-v1.html`.
- Email delivery appears after value. Transactional delivery and the optional Decision Watch are separate. Mindmaker Live remains a separate explicit subscription.
- The final implementation must contain one deduplicated Mindmaker design system. The approved prototype is visual evidence only. Its primitives must be merged into the canonical tokens and shared components, with old duplicates and temporary aliases removed after verified migration.
- A personalised watch is provisionally four weekly value-led updates followed by one final useful update about four weeks later. It requires its own clear opt-in and immediate stop rules.
- Mindmaker Live and the personalised watch are different subscriptions. Do not add a watch participant to Substack without a separate, explicit publication opt-in.
- For the first homepage direction, use only the approved media attendance logos: BBC, Hearst, and Condé Nast.
- Krish has authorised a later, narrowly scoped change to the Supabase intelligence after the new experience and output contract are agreed. Do not change it before that agreement.
- Public copy must be understandable to a ten-year-old, use British English, and contain no em dashes. Approved quotes remain verbatim.
- The proof document governs all brand, client result, and endorsement claims. Attendee brands are never described as clients.

## Authority and gates

- Authorised now: read-only product and traffic audit, local branch changes, tests, local renders, Relume exploration, a preview pull request, preview verification, and merge to `main` after the preview passes.
- Not authorised: changes to Supabase, the control centre, or the CTRL repository; production promotion; publishing private CTRL data.
- Product gate: interrogate the offer architecture, name, page journey, intelligent input, and copy with Krish before locking them.
- Visual gate: do not implement the material page layout until Krish explicitly approves a rendered, code-faithful mock based on the current design system.
- Release gate: stop after merge. Do not promote the build to production.

## Verification contract

- Product identity: public production revision must match the audited revision before conclusions are treated as current.
- Audit tasks: understand the current homepage, offer paths, intelligent tools, proof system, content routes, imagery, movement, and the public face of CTRL.
- Viewports: desktop at 1440 pixels and mobile at 390 pixels.
- Audit authority: public, read-only inspection only. Do not submit forms, invoke paid or private AI work, send messages, book meetings, or expose private CTRL data.
- Stop conditions: stop before any action that generates or stores user data, changes an external system, or needs new authority.
- Five-second test: a new visitor can explain what Mindmaker changes inside a business and identify the next step.
- Offer test: the public site presents one coherent paid engagement and does not leak retired offers or a public price.
- Interaction test: the hero tool produces useful, specific thinking that is clearly connected to Krish's method. A generic chat answer does not pass.
- Proof test: claims are supported, attendee brands cannot be mistaken for clients, and private or consent-gated material fails closed.
- Design test: desktop and mobile pages retain a distinctive Mindmaker character, useful movement, visible focus, good contrast, reduced-motion support, and no broken assets or browser errors.
- Delivery test: build and rebuild-specific tests pass, no new lint problems are added, and the matching preview revision passes before merge.

## Phase status

- Repositioning interview: complete and approved.
- Mock revision 1: rejected at the product rule, copy, and design-system level after the first reaction. Its clearer page order remains useful. It felt too generic and static, lost too much of the existing site's voice and movement, and was based on the superseded two-offer plan.
- Current truth: reopened and in progress against the new brief.
- Repository and production identity: confirmed for Mindmaker and CTRL.
- Product audit: in progress. The site currently contains overlapping sales paths, obsolete offer logic, and two separate intelligent inputs.
- Intelligence audit: in progress. Existing useful layers include company enrichment, current-company signals, curated and corroborated news, live model price data, conversational decision diagnosis, voice input, a structured decision brief, deterministic proof selection, a co-branded proposal, PDF output, and session handoff. The current `/intake` has strong company lookup, role-aware prompts, voice input, and visual structure, but remains a fixed 15-step old-offer form.
- Sensitive disclosure guard: complete for static public surfaces. The focused regression test passes. A stale copy remains in internal Mindy knowledge and must not return publicly.
- Positioning interrogation: next, after the evidence audit is complete.
- Relume exploration: complete for the homepage spine. Header 76 supplied the strongest asymmetric image composition; Layout 352 supplied a useful process rhythm. Relume's generated sitemap, generic consultancy copy, centred reveal headers, and default type system were rejected.
- Decision Underneath mock: result direction approved. The first standalone entry flow is no longer the homepage visual direction. Its useful interaction has been embedded and simplified inside the component-led homepage mock.
- Component-led homepage mock: rendered and verified at desktop and mobile sizes. The sample company path and result reveal work in place. Visual approval is pending.
- Comparison-led homepage mock V3: rejected. Do not refine this conceptual spine unless Krish explicitly asks for it.
- Preservation audit: in progress. Current strengths confirmed in the live local render include the cinematic ink hero, recognisable mint chrome, flip-card discovery, dark-light pace changes, real buyer language, expandable case depth, live price interpretation, intelligent inputs, voice support, haptics and the publication close.
- Signature interaction reset: `prototypes/mindmaker-decision-xray-v1.html` is the first rendered material mock on the new conceptual spine. It uses a complete worked example on first paint, reappropriates the current mint chrome and cinematic field, and places the decision directly under the selected Product, Pricing, Positioning or People lens. Visual approval is pending.
- Krish's first reaction accepted the Decision X-ray look but rejected its UX. It was still too complex, exposed the internal framework, felt like a generated answer and behaved like a separate page rather than a homepage demo.
- The compact in-hero business read is frozen as future concept work. It is not a current implementation task.
- Interactive demonstration work: paused by Krish on 12 August 2026.
- Material implementation without the demonstration: complete on the local branch. The current design system, cinematic hero, real imagery, motion, CTRL proof and dark-light page rhythm are retained and simplified around one Sprint and one booking action.

## Known decision and authority risks

- The current intelligent outputs are generated by Supabase Edge Functions whose prompts still route to retired offers and automation-heavy examples.
- A new visual shell can be built without Supabase changes, but a genuinely new intelligent result may require changing that prompt and output logic.
- Because Supabase changes are explicitly outside scope, this must be resolved with Krish as an authority choice or a separately approved handoff. It must not be hidden inside the website build.

## Next action

Review the local branch and open a preview pull request when authorised. Keep the interactive demonstration paused and stop before production promotion.

---

## 2026-08-19 superseding mock brief

This section supersedes the earlier public brand, offer, CTA and mock direction where they conflict. Earlier entries remain as useful decision history and negative evidence.

### Approved truth for the first Mindmake mock

- Public brand: `Mindmake` at `mindmake.co`.
- Publication: Mindmake at `content.mindmake.co`, presented on the site as `Useful ideas by email`.
- Product: `CTRL, a Mindmake product` at `ctrl.mindmake.co`.
- The supplied `Mindmake Logo.png` is the approved wordmark source. The existing owl icon and favicons remain unchanged.
- No standalone owl appears in the hero. The owl may appear as a compact identity mark and once as the closing signature.
- Public offer one: `Build your AI brain`, explained as capturing how a leader judges good work and turning it into a system the business owns.
- Public offer two: `Use AI to improve what you sell`, explained through product, price, message and team decisions, followed by working tools.
- The only public price line is: `Work starts at $1,500. You receive a fixed price before work begins.`
- The six-package pricing grid and all larger price bands stay internal.
- Every primary commercial action says `Book a fit call`.
- The fit action opens one short qualification state asking for company domain, work email and one choice: `Build how we think`, `Improve what we sell`, or `Not sure`.
- Calendly does not open automatically. Krish sends the booking link when the lead looks like a fit.
- Public copy uses British English, no em dashes and words a ten-year-old can follow. Do not use `anchor`, `surface`, `thesis` or unexplained `GTM`.
- Proof uses verified results, correctly labelled attendee organisations, real working images and real CTRL footage. CTRL is evidence, not a third service offer.
- This approval authorises a local visual mock only. It does not authorise material production implementation, domain work, deployment, redirects or asset deletion.

### Concept trace

Three materially different page spines were considered before the first mock:

1. **Editorial evidence file.** A calm promise with real working evidence assembled around it as the reader moves. Low user effort. Strong human and operator feel.
2. **Two working rooms.** The reader chooses `For you` or `For the business`, and most of the page changes emphasis. Clear choice, but too much interaction for a first visit.
3. **Decision to system to business.** A linear three-step story showing one judgment becoming a working system and then a business result. Clear mechanism, but at risk of feeling like a product tour.

Selected synthesis: the editorial evidence file carries the page. The decision-to-system progression appears only inside `How the work grows`. The two offers use a light, optional reveal rather than controlling the whole page. This creates useful movement without turning the site into a demo or making the reader learn a framework.

### Preservation contract for this mock

- Keep the current forest, paper, emerald and restrained rust palette.
- Keep the cinematic dark-light rhythm, real stage imagery, reliable motion primitives, accessible media behaviour and CTRL evidence.
- Replace the disparate Space Grotesk and Instrument-style pairing with one warm editorial display face and one calm humanist reading face.
- Replace repeated cards, carousels, blank video states, oversized accidental wraps and generic reveal animations.
- Do not remove production components or old assets during the mock stage.
- The rendered mock must be checked at 320, 390, 768 and 1440 pixels before visual review.

### Current phase status

- Sheet gate: approved for mock on 19 August 2026.
- Branch: `codex/mindmake-homepage-mock` from `2dfc7121969280c705925f3db2fc4e40a83197de`.
- Mock surface: homepage only, including the integrated first fit-screen state.
- Rendered artifact: `prototypes/mindmake-homepage-mock-v1.html`.
- Artifact SHA-256: `784B1DF49C4F185B2D4F357FB20DE00C7CAADC2325EA84331309204AA31DD28A`.
- Browser verification: 1440, 768, 390 and 320 pixel widths have no horizontal overflow, broken images, duplicate IDs, unnamed buttons, short visible buttons or console warnings and errors.
- Media verification: `CTRL-demo-aug-26.mp4` loads from the expected local path, exposes controls, shows a real poster, reports no media error and has a 36.37 second duration.
- Interaction verification: both offer examples open in place; the fit panel opens, traps and restores focus, shows the company read before email, and ends in an honest local success state without opening Calendly.
- Copy verification: no public `Mindmaker`, `Mindmaker Live`, em dash, `thesis`, `anchor`, `surface` or unexplained `GTM` remains. The only dollar figure is `$1,500` in the approved sentence.
- Build: passes. The full repository lint still reports 41 existing application and Supabase errors outside this standalone prototype; this mock adds no linted application source.
- Repository tests: 28 pass and 4 fail in the existing app and cookie-consent tests because the test environment supplies an invalid `localStorage`. The mock does not change application or test source. This is recorded, not fixed, during the visual mock gate.
- Reduced-motion coverage: the complete static page and media controls remain available; the source rule removes animations. Runtime media emulation was not available in the connected browser check and remains a production-build acceptance item.
- Material build, release and purge: blocked pending explicit visual approval.

### Progressive intelligence revision

- Krish approved showing a company-specific starting read before asking for email.
- The approved homepage spine remains unchanged.
- CTRL now has one optional proof reveal showing the inputs, checks and human call behind a decision.
- The fit path now has three states: company website, visible read, then email.
- The visible read separates what is known, what may be true and what still needs the leader's judgment.
- The mock includes a prepared media example and an honest fallback for any other domain. It does not claim to perform live research, store data or send email.
- The interaction has been rendered at 1440, 390 and 320 pixels. The drawer and CTRL reveal have no horizontal overflow, duplicate IDs, unnamed buttons or visible controls smaller than 44 pixels.
- The CTRL video loads fully in the browser with no media error.
- Production research, persistence, form delivery and deployment remain outside the mock authority.

### Deferred AI Brain and AI GTM page discovery

- The two public outcome territories remain `Build your AI brain` and `Use AI to improve what you sell`. They must not become generic pages about getting help with decisions.
- The AI Brain page must show an aspirational, human change. It should make it easy to picture a leader getting better at work they avoid, following through on work they dislike but need to do, encoding their judgment and taste, building a useful AI Chief of Staff, and filling gaps without losing the spark that put them in the seat.
- The AI GTM page must show how AI is changing important product, price, positioning, sales and company-shape decisions for internet, software and media businesses now. It must make clear that institutional knowledge from even a year ago may no longer be enough.
- Both pages need personified, real-world scenes. Show a recognisable leader, a specific pressure, the old way the decision worked, what AI changed, and the stronger action now available. Do not rely on abstract problem cards, generic AI claims or automation-consultant language.
- The visual system should make each change felt through the page. Use concrete before-and-after decisions, working artifacts and current evidence rather than decorative technology imagery.
- Each page must lead naturally to one contact action after enough value and recognition have been created.
- The current homepage problem and solution language is provisional. It is too close to `I help you make decisions` and does not yet carry the specific human promise of AI Brain or the commercial urgency of AI GTM.
- Before either page is mocked, run a detailed interrogation with Krish and current-source research. The AI GTM examples in particular must be checked against live product, pricing, buyer-behaviour and company-design evidence at build time.
- Open naming point: decide later whether `AI GTM` is a public label or only internal shorthand. Any public name must obey the plain-language rule and explain the term without relying on initials.
- Open expression point: `AI Chief of Staff` is a useful candidate example, not yet an approved headline or product promise.
- This is a deferred brief only. It does not authorise page mocks, homepage copy changes or production implementation.

### Product proof media rule

- Product proof will use current videos chosen for the final build: the agentic operating system at work, the CTRL brain at work and the decision engine at work.
- Do not draw circles, arrows, handwriting or explanatory overlays on product screenshots or product video frames.
- Do not choose an archived interface image or an available screenshot merely because it fills a visual slot. No temporary product frame may become the anchor for the page design.
- Hand-drawn marks may still explain an abstract result or decision diagram. They do not sit on product media.
- Build the product-media component so clips, posters and crops can be replaced without redesigning the page. The video is the evidence. The surrounding layout supplies context and controls without pretending the interface will stay fixed.
- Keep the tone calm and specific. Retire lines such as `We do not need fake images`, `not a deck, two real buyers` and `more than an AI answer`. They sound self-congratulatory and create an unnecessary comparison.
- `prototypes/mindmake-proof-motion-study-v1.html` is now a results-motion study only. Its annotated CTRL block and closing claim have been removed.
- Seven future media assets are now assumed in the page system.
  - Film: `Half Past Six` for AI Brain and the personal route.
  - Film: `Three Times Is A Rule` for memory, judgment and lasting improvement.
  - Film: `Two Conversations` for AI GTM and the business route.
  - Film: `Ask Me Anything I Did` for trust, governance and larger-company work.
  - CTRL clip: a decision gets sharper.
  - CTRL clip: the brain learns the leader's judgment.
  - CTRL clip: the leader takes their context into another AI tool.
- The homepage mock now uses one replaceable media contract. The two work routes have complete abstract fallback scenes, the archived Battle Test screenshot has been removed from the hero, and the current CTRL demo sits in a portrait proof frame behind a user-controlled play button.
- No page may show a blank video box, broken poster or `coming soon` treatment while final media is absent. A finished static or abstract state must remain useful on its own. Video replaces that state without moving the surrounding layout.
- The current `CTRL-demo-aug-26.mp4` is temporary proof only. It must be replaced by a clean native portrait capture without the black canvas, Recordify mark, retired name or private information before final publication.
- Poster frames, final crops and captions remain open until the produced assets arrive.

### Approved problem-section revision

- Krish approved the type-led problem-section direction on 19 August 2026 as a provisional copy treatment.
- The approved layout removes the stage photograph and movement control from the problem section and presents the three buyer pressures as full-width editorial rows.
- `krish-stage-2.png` moves into the hero with separate desktop and mobile focal positions. The existing V1 mock remains unchanged.
- Current local artifact: `prototypes/mindmake-homepage-mock-v2.html`.
- Artifact SHA-256: `2F9AFEFEEDDB3E40FD6B0DD126CFCDEDD2DF98FB234FA65B873E3F2A5BC5EF91`.
- Deterministic checks pass for exact copy presence, duplicate IDs, removed motion/image hooks, intended hero asset, mobile breakpoint rules and the no-em-dash rule.
- The repository production build passes. Existing bundle-size, ambiguous Tailwind-class and stale Browserslist warnings remain outside this mock change.
- V2 has now been rendered in the local browser at 390px and 1280px. At 390px it has no horizontal overflow, clipped headings, broken images, duplicate IDs, nested page components or sub-44px visible controls. The fit-call panel focuses its first field, both offer cards respond to touch, and CTRL play and pause work without a media error.
- The first phone pass found and corrected two real defects: the problem heading retained its desktop two-column grid and collided with its support copy; the fixed header stayed transparent through the full hero and could show a second call button beneath it. The corrected render has no text overlap and changes to the solid header after 24px of scroll.
- Mobile is not yet a release lock. At 390px the full page is about 14,340px tall, with the results section still carrying the largest stacking cost. A mobile-specific compression pass and physical iOS Safari and Android Chrome checks remain required before claiming the site works perfectly on the go.
- Final public wording remains open for Krish's later hard-write.
- Production implementation, merge and deployment remain unapproved.

### Next action

Review the V2 hero and sharper problem section as one material mock. If that direction is approved, produce one mobile-specific compression study that shortens the results, offer and proof journey without removing required trust. Keep the AI Brain and AI GTM pages at discovery stage until the detailed interrogation and current-source research are complete. Replace each fallback with its named asset when delivered, without changing page structure. Stop before production implementation or publication.

---

## 2026-08-19 mechanism-motion reset

This section supersedes the approved type-led problem layout and the abstract offer animations as the current visual proposal. It does not supersede the brand, offer, proof, media or authority contracts above.

### Why the reset happened

- The V2 problem section improved the words but remained a static editorial list.
- The AI Brain fallback reduced a $1,500-plus engagement to four blank blocks and one page.
- The AI GTM fallback used floating dots, a line and blank paper shapes without showing a business mechanism.
- Display headings repeatedly used narrow character measures that created stacks of short fragments.
- This was the second consecutive failure inside the same visual spine, so refinement stopped and a fresh divergence pass was run.

### Reset trace

- Sanitised brief: `MM-MOTION-RESET-R1`.
- Candidate one: `Both Sides of the Call`, based on comparing what the business believes with what the buyer now sees.
- Candidate two: `The Decision Table`, based on an operator proofing surface and linked records.
- Candidate three: `The Decision Press`, based on market evidence pulling product, price, message and team choices out of line.
- Diversity finding: the table and press were materially related physical-production concepts. The comparison concept was the independent spine.
- Judge one selected the comparison concept for its institutional memory, visible reasons and selective revision. Its main warning was that it could look like invented software or a worksheet.
- Judge two independently selected the same concept for mechanism clarity, mobile translation and reduced-motion resilience. Its main warning was that it could be strategically correct but visually underpowered.
- No tiebreaker was needed.
- Selected synthesis: use the comparison concept as the logic. Borrow only the tactile registration, ruled surfaces and material warmth from the physical concepts. Keep disagreement, sources and the human call visible. Never claim that AI produces a perfect decision.
- Reset count for this material surface: one.

### Selected motion contract

- Problem: one market change builds through product, price, message and team. The visitor can lightly emphasise the inside or buyer view, but both remain readable without the control.
- AI Brain: show many durable records across memory, standards, context and judgement. A live decision keeps conflicting evidence, sources and the human trade-off visible. A second decision proves reuse.
- AI GTM: show a current offer, readable market signals and linked working choices. Every changed choice keeps its reason. The ending is `ready to test`, not presented as fact.
- Motion is triggered by ordinary page scroll and changes the same visible object. There is no nested scroll, carousel, particle field, fake dashboard, chatbot or simulated AI oracle.
- Product footage remains clean and replaceable. No marks are placed on screenshots or video.
- The no-video and reduced-motion states are complete editorial compositions.

### Wrap contract

- Every display heading in the focused study carries an explicit rendered line-count limit.
- At 768 pixels and above, each audited heading must use no more than two lines.
- At 680 pixels and below, each audited heading must use no more than three lines.
- The layout widens the text measure or shortens the sentence before reducing it to fragments.
- `text-wrap: balance` supports the rule but does not replace the rendered audit.

### Current artifact and evidence

- Focused artifact: `prototypes/mindmake-mechanism-motion-study-v1.html`.
- Artifact SHA-256: `AA7F6B07002BE1FC486F36E71C3AFBB079D9B43A078CE6B8F28DFCAED444490D`.
- Scope: problem mechanism, AI Brain mechanism and AI GTM mechanism only. This is not a complete homepage revision.
- Rendered widths checked: 1440, 1280, 1024, 768, 390 and 320 pixels.
- All checked widths have zero horizontal overflow, no broken image, no duplicate ID, no unnamed button, no visible control below 44 pixels and no console or page error.
- All audited display headings pass the two-line desktop and three-line phone limits.
- Desktop scroll-state regression passes `0, 1, 2, 3` in order for all three chapters.
- The inside and buyer emphasis controls update the comparison state.
- Reduced motion loads every chapter in its complete final state and keeps all explanatory steps visible.
- The 390-pixel focused study is about 8,135 pixels tall. That is acceptable for reviewing the three mechanisms together, but it is not approval to transplant the full study unchanged into the homepage. The eventual mobile homepage must keep only the strongest beats.

### Authority and next gate

- This is a local design study only. It does not change the production site, routes, offers, prices, domain configuration, Supabase, CTRL, public assets or deployment state.
- The next gate is Krish's unanchored visual reaction to the focused study.
- If the mechanism is approved, extract the accepted beats into the full homepage mock and run a separate mobile compression pass. Do not implement the production page before that approval.

### Approval and full-page integration

- Krish approved the focused mechanism study on 19 August 2026 with the response `I love it`.
- The approved comparison logic, living decision memory and linked commercial choices are now integrated into a new full-homepage artifact: `prototypes/mindmake-homepage-mock-v3.html`.
- V1, V2 and the focused motion study remain unchanged so the three directions can still be compared.
- V3 removes the rejected blank-block AI Brain treatment, the particle-led AI GTM treatment and the static problem list from the visible page.
- The wide-screen problem story moves through states `0, 1, 2` in order. At 900 pixels and below it opens in the complete state and turns the three explanations into compact follow-on beats.
- The inside and buyer controls work independently of the scroll state. Both views remain readable when neither control is selected.
- The full-page heading audit now covers every visible `h1`, `h2` and `h3`, not only the focused study. It passes the three-line hero limit, the two-line wide-screen limit and the three-line phone limit.
- Rendered checks pass at 1440, 1280, 1024, 768, 390 and 320 pixels with no horizontal overflow, broken image, duplicate ID, heading-line failure, console error or page error.
- At 390 pixels there are no visible controls below 44 pixels. The complete decision state is present, all three explanation steps remain visible and the page has no blank media dependency.
- Keyboard focus is visible. The fit-call panel moves focus to its first field, traps focus while open, closes with Escape and restores focus to the trigger.
- The current CTRL video returns partial content as `video/mp4`, reaches browser ready state 4, plays behind its own control and keeps the finished fallback available.
- Reduced motion loads the complete decision state and collapses animation and transition duration to the browser's near-zero reduced-motion value.
- V3 artifact SHA-256: `BB468B02888278B10F414DC8E90931A26D9202D3426C46E1434F6EEF3A7E5D88`.
- The 390-pixel V3 page is about 15,075 pixels tall. Its visual mechanisms are compressed and usable, but the whole buying journey still needs an editorial deletion pass before anyone calls the mobile experience final.
- Final public wording, the seven replacement videos, physical iOS Safari and Android Chrome checks, production implementation, merge and deployment remain open and unapproved.

### Next action after approval

Review V3 as the new local source of truth for the homepage mock. If its full-page rhythm is approved, run the editorial deletion pass and hard-write the public copy before mapping the system into production React components. Keep all video slots replaceable and stop before production implementation or publication without a separate approval.

---

## 2026-08-20 production-baseline lock

This section supersedes earlier mock-version and route-architecture status where they conflict. Earlier records remain as design history and negative evidence.

### Locked visual baseline

- Production floor: `prototypes/mindmake-homepage-mock-v8.html`.
- V8 SHA-256: `CD58CE4B7FDCA9C49003F4AFE30158B7BDA78F16B62D9FADEEA871CAB311CA74`.
- Approved mechanism reference: `prototypes/mindmake-mechanism-motion-study-v1.html`.
- Mechanism SHA-256: `AA7F6B07002BE1FC486F36E71C3AFBB079D9B43A078CE6B8F28DFCAED444490D`.
- V8 is the V7 design spine plus later approved navigation, CTA, proof-layout and testimonial edge-case corrections. The V1 to V7 exploration must not be repeated during production implementation.

### Locked route architecture

- The homepage is the concise overview and choice point.
- `Build your AI brain` and the commercial AI outcome each open as a full route inside the same Mindmake application shell.
- These routes are not dialogs or overlays.
- Entry should feel continuous with the homepage, and return navigation should restore the visitor's prior page context where possible.
- The exact slugs remain open until the public naming and search-language check is complete.

### Current contradictions to reconcile

- Production React still presents the prior Mindmaker and 21-day Sprint architecture.
- `OFFERS.md` says the work must not become an open-ended retainer, which conflicts with the approved premium first-month proof leading to a three-month or longer relationship.
- Several design, route and publication documents still use retired names, URLs and CTA logic.
- V8 still displays a `$1,500` starting price, while the first-month economics and public price policy remain unresolved.
- V8 demonstrates an intelligent starting read locally, but live research, lead persistence, consent, email delivery and routing are not yet production-approved.

### Current phase and next action

- Phase: product and conversion decisions before the production specification.
- Locked: visual floor and full-route architecture.
- Next action owner: Krish and Codex.
- Next action: lock the premium first-month product, continuation shape and public price policy.
- Later gates: intelligence pipeline, publication system, production specification, implementation, verification, cleanup, preview PR, merge and separate production promotion approval.

### Public price lock

- The public site will show no monetary starting price.
- Remove the `$1,500` statement from the production specification and implementation.
- Public copy may state that work begins with a paid 30-day proof.
- Exact pricing is set privately after scope, client size, strategic value and any approved participation terms are understood.
- The internal cash floor, delivery capacity and continuation economics remain unresolved and must be locked before lead qualification is implemented.

### Updated next action

- Define the internal first-month cash floor and the amount of Krish's time one client may consume while preserving room for Mindmake's compounding assets.

### Delivery-capacity lock

- No more than two new 30-day proofs may run at the same time.
- Each proof may use 24 to 30 hours of Krish's time.
- A third paying customer must already be in a lighter continuation phase.
- Automated research, capture, synthesis and first drafts preserve Krish's time for judgment, relationships, challenge and the final standard.
- Work that cannot fit this envelope is custom, partner-supported or declined. It is not allowed to silently expand the standard proof.

### Updated next action

- Set the private cash floor and continuation economics from the approved time envelope and desired effective rate.

---

## 2026-08-23 production implementation authority

This section supersedes earlier next-action and implementation-authority lines where they conflict. Earlier sections remain design history and negative evidence.

### Current product lock

- Current commercial source: `project-documentation/MINDMAKE_CANON.md`.
- Public brand: Mindmake.
- Public doors: `Build Your AI Brain` and `Build Your AI GTM`.
- Paid start: one private-priced 30-day proof.
- Continuation: three months or longer when the work keeps finding useful opportunities.
- Capacity: two new proofs at once, 24 to 30 hours of Krish's time per proof.
- Premium value: extend what the leader can do. Task help and information delivery may support that result.
- Public message must include healthy, ambitious and high-growth companies as well as businesses under competitive pressure.

### Conversion lock

- No public Calendly or diary link.
- A company website is the first input.
- Show a useful, honest starting read before asking for email.
- Ask one easy pressure choice and one returned-capacity choice.
- Email gates the designed private brief, not the first useful result.
- Krish receives a rich company and fit digest and decides whether to reply.
- Newsletter consent is separate and unticked.
- No recurring personalised watch or nurture automation is part of the current build.

### Evidence and email boundary

- The production React repository has existing company-enrichment and lead-capture functions.
- Existing visitor-email functions still use retired Mindmaker, proposal and booking language. They are not approved for the new public promise.
- The local build may create the complete brief, provide a safe download and prepare the delivery adapter.
- A visitor-facing `sent by email` success claim stays disabled until a current Mindmake template is implemented and verified under separate backend authority.
- No Supabase code or live data is changed in this phase.

### New film

- `G:\My Drive\Ventures\Active\Mindmaker\04_Content\division-films\05-someone-i-already-know.md` is the privacy-safe AI Brain film brief.
- It proves that AI can make existing relationships usable while the leader keeps the judgment and contact.
- Real contact details, messages, names, employers, photographs, counts and unapproved vendor names are excluded.

### Baseline before production edits

- Revision: `2dfc7121969280c705925f3db2fc4e40a83197de` on `codex/mindmake-homepage-mock`.
- Worktree already contains the approved untracked V1 to V8 prototypes, mechanism studies, stage images and five CTRL demo clips, plus modified rebuild state and decision log. They are preserved.
- `npm run build`: passes.
- `npm run lint`: 41 errors and 20 warnings in the existing application and Supabase tree.
- `npm test -- --run`: 28 pass and 4 fail. The failures are the existing invalid `localStorage` test environment in `CookieConsent.test.tsx` and `App.test.tsx`.
- The rebuild may not add a lint error or a new failing test.

### Build contract

- `TARGET`: production React homepage, two full outcome routes, shared Mindmake shell and personalised brief flow.
- `CURRENT RUNTIME`: old Mindmaker 21-day Sprint site.
- `SOURCE OF TRUTH`: V8 visual floor, approved mechanism study and `MINDMAKE_CANON.md`.
- `AUTHORITY`: local code, local documentation and local evidence only.
- `PASS SIGNALS`: V8-level rhythm and finish, no public booking path, two real routes, value before email, separate unticked consent, no invented live intelligence, no aggressive heading wrap, responsive and reduced-motion pass.
- `ROLLBACK`: inspectable git diff on the current branch. No deletion or production mutation.
- `READBACK`: build, focused tests, lint comparison and rendered desktop/mobile checks.
- `STATUS`: local implementation and QA complete. Release gates remain open.

### Current phase

- Phase: local production implementation verified; preview and release gates remain.
- Locked material surface: V8.
- New material exploration is not required for the shared shell and homepage because Krish approved V8 as the floor and approved continuation into the build.
- Full-route copy remains subject to current-source evidence and later manual hard-writing, but route structure and the honest local conversion mechanism may be implemented now.

### Value-first product rule

- Every screen must teach, prove or help the visitor choose. Persuasion alone is not useful content.
- The guided brief must return value at every step. Company evidence, the chosen pressure, the human trade-off and the intended use of returned time all change the final brief and Krish's lead digest.
- Copy speaks to an experienced leader at their level. It does not preach, manufacture urgency or ask them to accept an unsupported claim.

### Historical verification readback at this implementation checkpoint

The figures below record this earlier checkpoint. They are superseded by the current snapshot at the top of this file.

- Shared Mindmake shell, homepage, Build Your AI Brain, Build Your AI GTM, results, contact, privacy and terms are implemented.
- The local personalised brief reads the company, lets the visitor choose a pressure and intended use of returned time, shows a useful preview before email, downloads a private HTML brief and prepares a rich lead digest.
- `npm run build`: passes.
- Focused Mindmake contract tests: 13 pass.
- Full test suite: 42 pass and the same 4 baseline `localStorage` environment failures remain.
- `npm run lint`: 41 errors and 19 warnings. The baseline was 41 errors and 20 warnings, so the rebuild adds none and removes one warning.
- Responsive matrix: 147 route and viewport combinations pass from 320 to 1920 pixels across all 21 indexed pages and articles.
- Full sitemap: 42 desktop and mobile combinations pass across all 21 indexed URLs.
- Live CTRL playback, reversible scroll movement, fixed testimonial controls, keyboard dialog behaviour, mobile navigation, reduced motion, company read, lead digest, private download and the worked org-chart hand-off pass.
- Full evidence: `project-documentation/MINDMAKE_REBUILD_QA_2026-08-23.md` and `C:\Users\krish\.scratch\mindmake-qa`.

### Historical next action at this checkpoint

At this earlier checkpoint, the planned next step was to review email, environment, legal, device, publication and legacy-source gates before a preview. The current snapshot at the top of this file supersedes that wording. This historical paragraph is not implementation authority.

---

## 2026-08-23 opening-act interaction reset

### Why this reset exists

- The last full-page hybrid mock was rejected. Its movement arrived at the wrong time, added little meaning and made the stronger older work feel generic.
- That rejected mock is preserved as negative evidence. It is not the visual floor and is not implementation authority.
- The recovery is deliberately narrow. No further full-site replacement is allowed until one opening interaction proves the direction.

### Concepts considered

- `Useful distance`: one promise stays fixed while the evidence changes around it.
- `The decision opens up`: a compact business question separates into its hidden choices.
- `The judgement thread`: one line carries a live change through product, price, message and team; evidence joins; the leader makes the call; the line then forks to Build Your AI Brain and Build Your AI GTM.

Two independent reviews selected `The judgement thread` because its movement changes the meaning at every step and connects most clearly to real CTRL proof. The selection is a working proposal, not owner approval.

### Current candidate

- Artifact: `prototypes/mindmake-judgement-thread-motion-study-v1.html`
- SHA-256: `6DF84FEB13C3F3FDEF5E41FDE52A99C9BC49F9880776426CF3F07B69A8F55C06`
- Scope: hero, five reversible motion beats, two-route fork and the hand-off into one real CTRL frame.
- The stage image never sits behind display copy.
- AI opens more paths, the four business choices pull apart, evidence joins, the rust human call resolves the tension and the same line becomes the proof frame and playhead.
- AI GTM moves outward towards opportunity. There is no decline curve or race-to-the-bottom message.
- The older hybrid and all earlier prototypes remain untouched.

### Verification

- Visual captures pass at 1440 by 900, 1280 by 800, 1024 by 768, 390 by 844 and 360 by 800.
- The sequence reverses when the visitor scrolls back.
- Short landscape, reduced motion, mobile menu rotation, keyboard route gating, route-to-proof switching and a JavaScript-free fallback pass.
- All five story chapters remain available to assistive technology. Hidden route links leave the tab order and return only when shown.
- There is no document-width overflow, headline/photo collision, reduced-motion section collision or browser error in the focused matrix.
- Evidence lives in `C:\Users\krish\.scratch\mindmake-qa`.

### Gate

This candidate now waits for Krish's visual and interaction judgement. It must not be copied into the production homepage, merged or deployed until that direction is approved. Approval of the concept will still require section-by-section visual decisions before the rest of the site is rebuilt.

---

## 2026-08-24 working-understanding category act

### Positioning evidence

- The older Mindmake site framed the real risk as losing the ability to judge and direct AI, not failing to buy enough tools.
- The MasterClass course `Revolutionize Your Workflows with AI` is useful category evidence for human aspiration, confidence and practical AI fluency. It is not evidence that any one line of copy converts, and its broad productivity framing is not the Mindmake position.
- Current reference: `https://www.masterclass.com/classes/revolutionize-your-workflows-with-ai`, reviewed 24 August 2026.

### Category decision

- Consultants can bring a plan.
- AI models can give an answer.
- Single-job tools can do one task.
- All-in-one systems can promise to run everything.
- Mindmake is not another card in that list. It helps the leader build enough working understanding to test, direct and decide.
- The public hinge is `You can hand over the work. Not the understanding.`
- Becoming obsolete is the private stake, not a public threat.

### Focused visual candidate

- Artifact: `prototypes/mindmake-judgement-thread-motion-study-v2.html`.
- V1 remains untouched.
- The new first act sits before the approved judgement thread inside the same `#judgement-thread` destination.
- Outside help arrives and does useful work. The act then brings the leader's own understanding back to the centre. Mindmake appears around the leader's ability to understand, check and decide, while outside help remains useful at the edge.
- The existing hero and judgement-thread markup are unchanged apart from the shared wrapper required for the new first act.

### Current verification

- Inline scripts parse.
- Hero source is byte-for-byte equal to V1.
- The existing judgement-thread content is unchanged; only the shared wrapper adds closing markup around it.
- Referenced image and video assets exist, the CTRL video metadata loads and the browser reports no runtime error.
- Rendered checks pass at 390, 768, 1024 and 1440 pixels with no document-width overflow.
- Reverse scroll restores the category state.
- Mobile and tablet collisions, the 1024-pixel heading stack and inactive-chapter accessibility were found, corrected and rerun.
- A separate positioning pass now passes the public category copy for tone, plain language and the intended MasterClass reference distinction.
- V2 is still a material visual candidate. It must not move into the production homepage, merge or deployment without Krish's explicit visual approval.

---

## 2026-08-25 approved judgement-thread floor

### Approval

- Krish reviewed `prototypes/mindmake-judgement-thread-motion-study-v5.html` and said, `I'm finally happy with this, do not regress from here`.
- This supersedes the candidate-only status of V1 and V2 for every surface also present in V5.

### Approved artifact

- Artifact: `prototypes/mindmake-judgement-thread-motion-study-v5.html`.
- SHA-256: `DE09D75C46EB660AD6148C1D7F5DD61E4F82031B48FCFE931CC3AE05C8126C81`.
- Scope: homepage opening, working-understanding act, judgement thread, Brain and GTM gateway, first CTRL proof, responsive composition and reversible motion.
- V8 remains the production breadth reference for complete routes, testimonial range and proof that V5 does not contain. V5 is the stronger visual authority where the two overlap.

### What is locked

- Motion must change what the visitor understands. It cannot exist only to make the page move.
- The old rotating hero questions remain the current emotional recognition device. They start quickly, fit inside the first viewport and use a plain `Start here` action.
- The Brain and GTM gateway is its own event. Its cards pull apart from one shared centre and move back when the visitor scrolls up.
- The two route names are `Build Your AI Brain` and `Build Your AI GTM`.
- The route gateway heading is `Ways to get started on your AI journey`.
- CTRL proof is factual and useful. It does not use cryptic status copy, invented artifacts or copy that tells the visitor what to think.
- Display copy does not overlap the stage image and does not break into accidental narrow stacks.

### Global eyebrow rule

- Eyebrows are banned across the full public system, not just in the reviewed sections.
- The ban includes kickers, overlines, chapter labels, decorative counters, proof badges, status straps and small pre-headings placed above or beside a real heading.
- Renaming the class, changing the case or moving the same device into a diagram does not satisfy the rule.
- A small label is allowed only when it directly names an object, control, value or axis and is needed to understand or use it. It cannot act as decorative preamble.
- The accepted V5 source and live DOM both returned zero forbidden eyebrow-pattern matches at approval time.

### Why the rule exists

- Repeated pre-headings made the hierarchy feel templated and AI-generated.
- They forced the visitor to decode a redundant label before reaching the actual point.
- Removing individual examples did not work because the pattern remained in the component architecture and returned under different names.
- The correction therefore applies to the function of the pattern, not a list of banned words or CSS classes.

### Verification at approval

- Source and live DOM scans returned zero eyebrow, kicker, overline, chapter-number, decorative-counter, proof-status or pre-heading patterns.
- The rotating hero paused without changing and resumed while the control retained focus.
- The Brain and GTM cards moved apart on downward scroll and returned to their starting position on upward scroll.
- The 1280 by 720 desktop view and 390 by 844 mobile views showed no document-width overflow.
- Mobile proof copy no longer formed an accidental narrow stack.
- Browser console output contained no application error.

### Authority and next use

- This approval authorises V5 as the local visual and interaction floor and as the comparison baseline for the next mock and production integration.
- It does not authorise deployment, merge, public route changes, email wiring, Supabase changes, domain changes, deletion or production promotion.
- Future work must compare the affected surface with V5 and preserve every approved strength before it can replace it.

---

## 2026-08-26 frozen gateway and source-only private brief V2

### Exact visual contract

- Candidate 7 V2 is the approved Brain and GTM gateway, not a direction to redraw. The prototype, React component and stylesheet remain byte-stable contracts.
- Approved files and SHA-256 values:
  - `prototypes/mindmake-brain-gtm-gateway-candidate-7-v2.html`: `5A3F68994BFBD2AF412D95776515CF8F7884150FE49991364DDC680E3B418E42`.
  - `src/components/mindmake/BrainGtmGateway.tsx`: `E95327BD7FFDC12071558CF05355411071FABC54AEE548D335EB32A07872017F`.
  - `src/styles/mindmake-gateway.css`: `1A3440796054A668191B4D54DC27C8D601C480E31E546E69D3300A6D9A8A2EBC`.
- V5 remains the minimum standard for the opening act, judgement sequence, first CTRL proof, responsive composition and reversible causal motion.
- Eyebrows remain prohibited everywhere except the approved `Pick your starting point` label inside the frozen gateway.
- The wordmark and every homepage section share one measured content edge. Device fit is achieved by recomposing the scene, not clipping content, hiding actions or shrinking touch targets.
- Motion must explain the claim. Customer outcomes, attendance proof and career testimonials keep separate meanings and do not reappear in duplicate sections.

### Conversion contract

- The main action is `Start here`. There is no public diary link.
- The visitor receives a useful company read and recommendation before contact is requested.
- The browser sends only identifiers: verified-contact input, company domain and allowlisted choice IDs. It does not send its own narrative, recommendation or fit judgement.
- The server owns research, brief assembly, verification, visitor delivery and Krish's fit summary.
- Publication interest is a separate, unticked expression of interest. It is not a subscription and does not authorise a manual or automatic import.

### Current implementation truth

- The version-two migration, Edge Function, visitor email and operator digest exist in source.
- `VITE_MINDMAKE_BRIEF_HANDOFF_ENABLED` remains off. The source has not been applied to a preview database, deployed, or proved with real provider acceptance and synthetic inboxes.
- The flag cannot change until preview end-to-end checks pass, the retention schedule and any promised cleanup exist, exact allowed origins and symbolic secret references are configured, and the private database boundary is independently verified.
- Provider acceptance is not inbox delivery. The interface and emails may report only the state the server actually returned.

### Release boundary

This checkpoint authorises internal canon and source verification only. It does not authorise a migration, Edge Function deployment, flag change, live email, merge, domain change, deletion or production promotion.
